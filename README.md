# Subscription Bot

**Telegram-бот для управления подписками на каналы с прогрессивным доступом**

---

## Что делает этот бот?

Представьте: у вас есть основной канал и несколько дополнительных каналов с эксклюзивным контентом. Вы хотите, чтобы подписчики получали доступ к дополнительным каналам **постепенно** — чем дольше человек подписан, тем больше контента ему доступно.

**Пример:**
- Человек подписался на ваш основной канал
- Через 32 дня — получает доступ к "Модуль 1"
- Через 62 дня — получает доступ к "Модуль 2"
- Через 93 дня — получает доступ к "Модуль 3"
- И так далее...

Если человек отписывается от основного канала — доступ ко всем дополнительным каналам **автоматически отзывается**.

---

## Возможности

### Для пользователей
- Автоматическая регистрация при подписке
- Красивый прогресс-бар до следующего канала
- Уведомления о скором открытии нового канала
- Настройка уведомлений (можно отключить)

### Для администраторов
- Детальная статистика (Retention, Churn rate)
- Логи всех действий пользователей
- Гибкое управление каналами
- Массовые операции (выдача/отзыв доступа)
- Ручная выдача доступа (платные ускорения)
- Рассылка сообщений

---

## Пошаговая настройка

### Шаг 1: Создание бота в Telegram

1. Откройте Telegram и найдите **@BotFather**
2. Напишите ему `/newbot`
3. Введите **имя бота** (это то, что увидят пользователи), например: `Подписки на каналы`
4. Введите **username бота** (уникальный, заканчивается на `bot`), например: `my_subscription_bot`
5. **BotFather пришлёт вам токен** — это длинная строка вида:
   ```
   123456789:ABCdefGHIjklMNOpqrsTUVwxyz
   ```
   **Сохраните этот токен! Он понадобится позже.**

### Шаг 2: Узнаём свой Telegram ID (для админки)

Чтобы пользоваться админ-панелью бота, нужно знать свой Telegram ID.

1. Найдите в Telegram бота **@userinfobot**
2. Напишите ему `/start`
3. Он пришлёт ваш ID — это число, например: `123456789`
4. **Запишите этот ID**

### Шаг 3: Создаём каналы (если ещё нет)

Вам нужны:
- **1 основной (материнский) канал** — на него подписываются пользователи
- **1-4 дополнительных канала** — доступ к ним будет выдаваться постепенно

Для каждого канала:
1. Создайте канал в Telegram (или используйте существующий)
2. Канал должен быть **приватным**

### Шаг 4: Узнаём ID каналов

Это самый важный шаг! ID канала — это отрицательное число вида `-1001234567890`.

**Способ 1: Через @username_to_id_bot**
1. Найдите бота **@username_to_id_bot** в Telegram
2. Перешлите ему любое сообщение из вашего канала
3. Бот пришлёт ID канала

**Способ 2: Через веб-версию Telegram**
1. Откройте https://web.telegram.org
2. Зайдите в нужный канал
3. Посмотрите на URL в браузере — там будет число
4. Добавьте `-100` в начало этого числа

**Способ 3: Через @RawDataBot**
1. Добавьте @RawDataBot в канал
2. Напишите любое сообщение в канале
3. Бот покажет JSON с данными, найдите `"chat": {"id": -100...}`
4. Удалите бота из канала после получения ID

**Запишите ID всех каналов!**

### Шаг 5: Добавляем бота в каналы как администратора

**Это обязательно!** Бот должен быть админом во ВСЕХ каналах.

Для каждого канала:
1. Откройте канал
2. Нажмите на название канала вверху
3. Нажмите "Администраторы" → "Добавить администратора"
4. Найдите вашего бота по username (например, `@my_subscription_bot`)
5. **Выдайте боту следующие права:**
   - ✅ Приглашать пользователей по ссылке
   - ✅ Блокировать пользователей
   - ✅ Добавлять новых администраторов (опционально)
6. Нажмите "Сохранить"

**Повторите для ВСЕХ каналов** (и основного, и дополнительных).

### Шаг 6: Скачиваем и настраиваем бота

#### Вариант А: Если у вас есть Git

```bash
# Клонируем репозиторий
git clone https://github.com/maksimblak/Subscription_bot.git

# Переходим в папку
cd Subscription_bot
```

#### Вариант Б: Если нет Git

1. Зайдите на https://github.com/maksimblak/Subscription_bot
2. Нажмите зелёную кнопку "Code" → "Download ZIP"
3. Распакуйте архив
4. Откройте папку в терминале

### Шаг 7: Создаём файл конфигурации

1. В папке проекта найдите файл `.env.example`
2. Скопируйте его и назовите `.env`
3. Откройте `.env` в текстовом редакторе
4. Заполните все поля:

```env
# Токен бота (получили в Шаге 1)
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# Ваш Telegram ID (получили в Шаге 2)
# Можно указать несколько через запятую: 123456789,987654321
ADMIN_IDS=123456789

# ID основного (материнского) канала
MAIN_CHANNEL_ID=-1001234567890

# Дополнительные каналы
# CHANNEL_X_ID — ID канала
# CHANNEL_X_NAME — название (любое, для красоты)
# CHANNEL_X_DAYS — через сколько дней открывается (0 = сразу)
# CHANNEL_X_EMOJI — эмодзи (необязательно)

CHANNEL_1_ID=-1001111111111
CHANNEL_1_NAME=Модуль 1
CHANNEL_1_DAYS=0
CHANNEL_1_EMOJI=📚

CHANNEL_2_ID=-1001222222222
CHANNEL_2_NAME=Модуль 2
CHANNEL_2_DAYS=32
CHANNEL_2_EMOJI=📖

CHANNEL_3_ID=-1001333333333
CHANNEL_3_NAME=Модуль 3
CHANNEL_3_DAYS=62
CHANNEL_3_EMOJI=📕

CHANNEL_4_ID=-1001444444444
CHANNEL_4_NAME=Модуль 4
CHANNEL_4_DAYS=93
CHANNEL_4_EMOJI=📗

# Время ежедневной проверки (когда бот проверяет подписки)
# По умолчанию 10:00 утра
SCHEDULER_HOUR=10
SCHEDULER_MINUTE=0
```

### Шаг 8: Запуск бота

#### Вариант А: Через Poetry (рекомендуется)

```bash
# Устанавливаем Poetry (если нет)
pip install poetry

# Устанавливаем зависимости
poetry install

# Запускаем бота
poetry run python main.py
```

#### Вариант Б: Через pip

```bash
# Создаём виртуальное окружение
python -m venv venv

# Активируем его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt

# Запускаем бота
python main.py
```

#### Вариант В: Через Docker

```bash
# Создаём папку для базы данных
mkdir data

# Запускаем
docker-compose up -d

# Смотрим логи
docker-compose logs -f
```

### Шаг 9: Проверяем, что всё работает

1. Откройте вашего бота в Telegram
2. Напишите `/start`
3. Если вы подписаны на основной канал — бот должен вас зарегистрировать
4. Напишите `/admin` — должна открыться админ-панель

**Поздравляю! Бот работает!**

---

## Как пользоваться

### Команды для пользователей

| Команда | Что делает |
|---------|------------|
| `/start` | Регистрация в боте |
| `/status` | Показывает прогресс и открытые каналы |
| `/channels` | Получить ссылки на доступные каналы |
| `/settings` | Настроить уведомления |

### Команды для администраторов

| Команда | Что делает |
|---------|------------|
| `/admin` | Открыть админ-панель |
| `/stats` | Быстрая статистика |
| `/broadcast <текст>` | Отправить сообщение всем пользователям |

### Админ-панель

В админ-панели (`/admin`) доступно:

- **Статистика** — сколько пользователей, сколько активных
- **Аналитика** — Retention, Churn rate, статистика по дням
- **Пользователи** — список, поиск, массовые операции
- **Каналы** — изменить количество дней для открытия
- **Логи** — последние действия в боте

### Ручная выдача доступа (платные ускорения)

Если пользователь хочет получить доступ к каналу раньше срока (например, за деньги):

1. Откройте `/admin`
2. Нажмите "Пользователи"
3. Нажмите "Выдать доступ"
4. Введите Telegram ID пользователя
5. Выберите канал для выдачи

---

## Как это работает внутри

### Регистрация пользователя
1. Пользователь подписывается на основной канал
2. Пишет боту `/start`
3. Бот проверяет подписку через Telegram API
4. Сохраняет пользователя в базу данных
5. Сразу выдаёт доступ к каналам с `days_required = 0`

### Ежедневная проверка
Каждый день в заданное время бот:
1. Проверяет всех активных пользователей
2. Считает, сколько дней человек подписан
3. Если накопилось достаточно дней — создаёт invite link и отправляет пользователю
4. Предупреждает о скором открытии новых каналов

### Отписка от основного канала
Когда пользователь отписывается:
1. Бот получает уведомление от Telegram
2. Удаляет пользователя из всех дополнительных каналов
3. Удаляет все сообщения с invite-ссылками
4. Отмечает пользователя как неактивного

### Повторная подписка
Если пользователь подписался снова:
1. Отсчёт дней начинается заново
2. Нужно снова накопить дни для доступа к каналам

---

## Структура проекта

```
Subscription_bot/
├── main.py              # Запуск бота
├── config.py            # Настройки
├── database/
│   ├── db.py            # Подключение к базе
│   └── models.py        # Работа с данными
├── handlers/
│   ├── user.py          # Команды пользователей
│   └── admin.py         # Админ-панель
├── services/
│   ├── subscription.py  # Логика подписок
│   └── scheduler.py     # Планировщик
├── utils/
│   ├── helpers.py       # Вспомогательные функции
│   └── messages.py      # Тексты сообщений
├── Dockerfile
├── docker-compose.yml
└── pyproject.toml       # Зависимости (Poetry)
```

---

## Часто задаваемые вопросы (FAQ)

### Бот не отвечает на /start
- Проверьте, что бот запущен (в консоли должны быть логи)
- Проверьте, правильный ли BOT_TOKEN в `.env`
- Посмотрите ошибки в консоли

### Бот говорит "Подписка не найдена"
- Убедитесь, что вы подписаны на основной канал
- Проверьте, правильный ли MAIN_CHANNEL_ID в `.env`
- Убедитесь, что бот является администратором основного канала

### Бот не может создать invite link
- Проверьте, что бот — админ в канале
- Проверьте, что у бота есть право "Приглашать пользователей"
- Проверьте правильность ID канала в `.env`

### Не работает /admin
- Проверьте, что ваш Telegram ID указан в ADMIN_IDS
- ID должен быть числом, без пробелов

### Как узнать свой Telegram ID?
- Напишите боту @userinfobot команду /start
- Или перешлите любое сообщение боту @RawDataBot

### Как добавить больше каналов?
Добавьте в `.env`:
```env
CHANNEL_5_ID=-100...
CHANNEL_5_NAME=Название
CHANNEL_5_DAYS=120
CHANNEL_5_EMOJI=📘
```

### Как изменить количество дней для канала?
1. Через админ-панель: `/admin` → Каналы → Изменить дни
2. Или измените значение `CHANNEL_X_DAYS` в `.env` и перезапустите бота

### База данных где хранится?
- При локальном запуске: файл `bot_database.db` в папке проекта
- При запуске через Docker: в папке `data/`

### Как сделать бэкап?
Скопируйте файл `bot_database.db` (или `data/bot_database.db` для Docker)

---

## Требования

- Python 3.10 или новее
- Telegram бот с правами администратора во всех каналах
- Стабильный интернет (бот должен работать постоянно)

## Технологии

- **aiogram 3.x** — асинхронный фреймворк для Telegram ботов
- **aiosqlite** — асинхронная работа с SQLite
- **APScheduler** — планировщик задач
- **Poetry** — управление зависимостями
- **Docker** — контейнеризация

---

## Поддержка

Если что-то не работает:
1. Проверьте FAQ выше
2. Посмотрите логи бота (ошибки подсвечиваются красным)
3. Создайте Issue на GitHub

---

## Лицензия

MIT — делайте что хотите, но на свой страх и риск.
